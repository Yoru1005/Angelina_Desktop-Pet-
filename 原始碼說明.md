# Angelina 桌寵原始碼

## 檔案內容

- `angelina_pet.py`：桌寵主程式，使用 PySide6 建立透明置頂視窗、動畫與中文右鍵選單。
- `assets/`：已去除綠幕的動畫影格及 `manifest.json`。
- `prepare_assets.py`：從原始 GIF 重新轉換透明動畫影格的工具。
- `build.ps1`：安裝依賴並封裝成單一 Windows `.exe`。
- `requirements.txt`：Python 套件依賴。

## 執行原始碼

請先安裝 64 位元 Python 3.10 以上版本，接著在 PowerShell 執行：

```powershell
py -m pip install Pillow
py .\angelina_pet.py
```

## 重新封裝 EXE

在 PowerShell 執行：

```powershell
powershell -ExecutionPolicy Bypass -File .\build.ps1
```

完成後的程式位於 `dist\Angelina桌寵.exe`。

## 操作方式

- 左鍵拖曳：移動桌寵
- 滑鼠滾輪：調整大小
- 雙擊左鍵：隨機切換動作
- 右鍵：在桌寵旁邊開啟中文功能選單
- 功能選單：選擇動作、縮放、切換置頂、查看操作說明或退出

桌寵介面已統一使用 PySide6，避免原生 Windows 透明視窗與右鍵選單互相遮擋。目前的打包與操作流程以 Windows 為主。
